#include <iostream>
#include <string>
#include <Windows.h>
#include "plateau.h"



using namespace std;

plateau::plateau() {
    
}
/*
void plateau::afficherPlateau( int hauteur, int largeur){

	

    system("cls");


    
    cout << endl << endl << endl;
    
    for (hauteur = 5; hauteur >= 0; hauteur--)
    {
        cout << "          | ";
        for ( largeur = 0; largeur <= 6; largeur++)
        {
            if (tab[largeur][hauteur] != 0 && tab[largeur][hauteur] != 1)
                cout << "  ";

            if (tab[largeur][hauteur] == 0)
                cout << "X ";

            if (tab[largeur][hauteur] == 1)
                cout << "O ";

            //cout << " ";
        }
        cout << "|" << endl << "          |---------------|" << endl;
    }
    cout << "          | 1 2 3 4 5 6 7 |" << endl;
    cout << "           ______________" << endl << endl;
}
*/
